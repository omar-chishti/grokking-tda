# @package _global_
# MLP grokking reproduction on modular addition mod 97 (architecture-robustness track).
defaults:
  - override /model: mlp
  - override /data: mod_add_p97
  - override /train: full_batch_adamw

seed: 0
train:
  steps: 40000
  optimizer:
    weight_decay: 1.0
