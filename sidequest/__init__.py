"""R24, a side quest: the operator as a transformation.

Separate from ``analysis/`` on purpose. Nothing here is imported by the thesis pipeline, and it
reads ``results-sidequest/`` rather than ``results/``, so no number in the manuscript can move
because of anything in this package.
"""
