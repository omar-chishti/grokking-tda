"""Command-line entry points (Hydra apps).

``train``   — compose a config, run the engine, write artifacts.
``analyse`` — load a run's artifacts, compute observables, write an analysis table.
``plot``    — render vector-first figures from a run's metrics/observables.
"""
